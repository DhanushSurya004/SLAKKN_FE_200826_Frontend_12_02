import { useEffect,useState } from 'react'; import { getLeaderboard,getTopicLeaderboard,getTopics } from '../api/quizApi';
export default function Leaderboard(){const [topics,setTopics]=useState([]),[rows,setRows]=useState([]),[selected,setSelected]=useState(''),[error,setError]=useState(''); useEffect(()=>{getTopics().then(setTopics).catch(()=>setError('Could not connect to the backend.'));},[]);useEffect(()=>{const request=selected?getTopicLeaderboard(selected):getLeaderboard();request.then(setRows).catch(e=>setError(e.response?.data?.message||'Could not load leaderboard.'));},[selected]);return <section><p className="eyebrow">THE HALL OF FAME</p><h2>Leaderboard</h2><div className="leaderbar"><p>Top scores, fastest finishes.</p><select value={selected} onChange={e=>setSelected(e.target.value)}><option value="">All topics</option>{topics.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></div>{error?<p className="error">{error}</p>:<div className="table-wrap"><table><thead><tr><th>Rank</th><th>Player</th><th>Topic</th><th>Score</th><th>Time</th><th>Date</th></tr></thead><tbody>{rows.length?rows.map(row=><tr key={`${row.rank}-${row.createdAt}`}><td className="rank">#{row.rank}</td><td>{row.playerName}</td><td>{row.topicName}</td><td><b>{row.score}</b></td><td>{row.timeTaken} sec</td><td>{new Date(row.createdAt).toLocaleDateString()}</td></tr>):<tr><td colSpan="6" className="empty">No results yet. Be the first to play!</td></tr>}</tbody></table></div>}</section> }
