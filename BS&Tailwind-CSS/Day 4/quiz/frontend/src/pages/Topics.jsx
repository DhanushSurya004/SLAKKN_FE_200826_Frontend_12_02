import { useEffect, useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { getTopics } from '../api/quizApi'; import { useQuiz } from '../context/QuizContext';
const icons = ['◎','⚗','⌘','⌛','◈','⚽','◉','✦'];
export default function Topics() { const [topics,setTopics]=useState([]),[error,setError]=useState(''); const navigate=useNavigate(); const {playerName}=useQuiz(); useEffect(()=>{if(!playerName) navigate('/'); else getTopics().then(setTopics).catch(e=>setError(e.response?.data?.message || 'Could not load topics. Is the backend running?'))},[playerName,navigate]); return <section><p className="eyebrow">PLAYER: {playerName}</p><h2>Choose your arena</h2><p className="subtext">Ten questions. One minute. Make every answer count.</p>{error ? <p className="error">{error}</p> : <div className="topic-grid">{topics.map((topic,i)=><article className="topic-card" key={topic.id}><span>{icons[i]}</span><h3>{topic.name}</h3><button className="card-action" onClick={()=>navigate(`/quiz/${topic.id}`,{state:{topic}})}>START →</button></article>)}</div>}</section> }
