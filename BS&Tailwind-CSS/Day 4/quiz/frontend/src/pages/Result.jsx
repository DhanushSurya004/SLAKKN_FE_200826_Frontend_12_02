import { useNavigate } from 'react-router-dom'; import { useQuiz } from '../context/QuizContext';
export default function Result(){const {result,setResult}=useQuiz();const navigate=useNavigate();if(!result)return <section><h2>No quiz result yet.</h2><button className="button" onClick={()=>navigate('/')}>GO HOME</button></section>; const again=()=>{setResult(null);navigate('/topics')}; return <section className="result"><p className="eyebrow">QUIZ COMPLETE</p><h1>Nice work,<br/><em>{result.playerName}!</em></h1><div className="score-ring"><strong>{result.score}</strong><span>/ 100</span></div><div className="result-stats"><div><b>{result.correctAnswers} / {result.totalQuestions}</b><span>Correct answers</span></div><div><b>{result.timeTaken} sec</b><span>Time taken</span></div></div><div className="result-actions"><button className="button" onClick={again}>PLAY AGAIN →</button><button className="outline-button" onClick={()=>navigate('/leaderboard')}>VIEW LEADERBOARD</button></div></section> }
